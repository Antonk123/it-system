{
  "name": "@it-system/codemap",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "extract": "tsx src/extract/index.ts",
    "merge": "tsx src/merge.ts",
    "build": "tsx src/build.ts",
    "generate": "npm run extract && npm run merge && npm run build",
    "test": "vitest run"
  },
  "dependencies": {
    "ts-morph": "^23.0.0",
    "yaml": "^2.5.0",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "tsx": "^4.19.0",
    "typescript": "^5.5.0",
    "vitest": "^2.1.0",
    "@types/node": "^20.14.0"
  }
}
