---
phase: 01-reports-fix-improvements
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - src/pages/Reports.tsx
  - src/hooks/useReportsPreferences.ts
autonomous: true
requirements: [RPT-02, RPT-03]

must_haves:
  truths:
    - "Reports page charts display data from the full ticket dataset via useReportsSummary hook"
    - "A horizontal bar chart in the Oversikt tab shows ticket counts per category"
    - "The Trend tab shows a ComposedChart with created bars and closed line overlay"
    - "useTickets import is removed from Reports.tsx — all aggregation flows through useReportsSummary"
  artifacts:
    - path: "src/pages/Reports.tsx"
      provides: "Reports page wired to new endpoint with category chart and trend overlay"
      contains: "useReportsSummary"
    - path: "src/hooks/useReportsPreferences.ts"
      provides: "Updated module registry with categoryChart entry"
      contains: "categoryChart"
  key_links:
    - from: "src/pages/Reports.tsx"
      to: "src/hooks/useReportsSummary.ts"
      via: "useReportsSummary(selectedYear, selectedMonth)"
      pattern: "useReportsSummary\\("
    - from: "src/pages/Reports.tsx"
      to: "recharts ComposedChart"
      via: "import { ComposedChart, Line }"
      pattern: "ComposedChart"
---

<objective>
Wire Reports.tsx to the new useReportsSummary hook, add the category breakdown horizontal bar chart in the Oversikt tab, and replace the existing trend bar chart with a ComposedChart showing both created (bars) and closed (line).

Purpose: Delivers RPT-02 (category chart) and RPT-03 (open/closed trend overlay) on top of the corrected data foundation from Plan 01.
Output: Modified `Reports.tsx` and `useReportsPreferences.ts`
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-reports-fix-improvements/01-CONTEXT.md
@.planning/phases/01-reports-fix-improvements/01-RESEARCH.md
@.planning/phases/01-reports-fix-improvements/01-UI-SPEC.md
@.planning/phases/01-reports-fix-improvements/01-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 outputs -->

From src/hooks/useReportsSummary.ts (created in Plan 01):
```typescript
export interface ReportsSummary {
  totals: {
    open: number; inProgress: number; waiting: number;
    resolved: number; closed: number; total: number;
  };
  byCategory: { category: string; count: number }[];
  trend: { month: string; created: number; closed: number }[];
  avgResolutionDays: number;
  agingTickets: number;
}

export const useReportsSummary = (year?: string, month?: string) => {
  return useQuery<ReportsSummary>({ ... });
};
```

From src/hooks/useReportsPreferences.ts (current state):
```typescript
export type ReportModuleId =
  | 'requesterAnalytics'
  | 'statusDistribution'
  | 'priorityChart'
  | 'monthlyChart'
  | 'activityHeatmap'
  | 'statusFlow'
  | 'tagAnalytics';
// categoryChart must be added to this union
```

From src/pages/Reports.tsx (existing constants, lines 26-63):
```typescript
const COLORS = [
  'hsl(var(--primary))',
  'hsl(var(--chart-2))',
  'hsl(var(--chart-3))',
  'hsl(var(--chart-4))',
  'hsl(var(--chart-5))',
];

const STATUS_COLORS: Record<string, string> = {
  open: 'hsl(var(--chart-1))',
  'in-progress': 'hsl(var(--chart-2))',
  // ...
};

const MONTH_NAMES = ['Januari', 'Februari', ...];
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire Reports.tsx to useReportsSummary and add category chart + trend overlay</name>
  <files>src/pages/Reports.tsx, src/hooks/useReportsPreferences.ts</files>
  <read_first>
    - src/pages/Reports.tsx (FULL FILE — understand complete tab structure, existing useMemo blocks, filter state, all chart sections, KPI card wiring)
    - src/hooks/useReportsSummary.ts (the hook created in Plan 01 — confirm exports and ReportsSummary shape)
    - src/hooks/useReportsPreferences.ts (full file — understand ReportModuleId union and DEFAULT_MODULES array)
    - .planning/phases/01-reports-fix-improvements/01-UI-SPEC.md (visual specs for both new charts)
  </read_first>
  <action>
This task makes three coordinated changes to Reports.tsx and one change to useReportsPreferences.ts.

**A. Replace useTickets with useReportsSummary (per D-06, D-07):**

1. Remove the `import { useTickets } from '@/hooks/useTickets'` line.
2. Add `import { useReportsSummary } from '@/hooks/useReportsSummary'` and `import { ComposedChart, Line } from 'recharts'` (add to existing recharts import).
3. Add `import { Printer } from 'lucide-react'` to existing lucide import.
4. Replace the `useTickets()` call (around line 151) with `const { data: summary, isLoading, isError } = useReportsSummary(selectedYear, selectedMonth);`
5. Remove ALL `useMemo` blocks that aggregate from the raw `tickets` array — these include: status counts, monthly chart data, priority distribution, aging tickets, etc. Replace each with direct reads from `summary.totals`, `summary.byCategory`, `summary.trend`, `summary.avgResolutionDays`, `summary.agingTickets`.
6. `availableYears` for the year dropdown: derive from `summary?.trend` — extract unique YYYY prefixes from `trend[n].month` strings. If trend is empty or summary not loaded, default to `[String(new Date().getFullYear())]`.
7. Add loading state: when `isLoading`, show `<Skeleton className="h-24 w-full" />` blocks (4 in a grid for KPI cards, one `<Skeleton className="h-[300px] w-full" />` for each chart area). Import `Skeleton` from `@/components/ui/skeleton`.
8. Add error state: when `isError`, show `<Alert variant="destructive">` with heading "Kunde inte ladda rapportdata" and body "Kontrollera anslutningen och ladda om sidan." Import `Alert, AlertDescription, AlertTitle` from `@/components/ui/alert`.

**B. Add category breakdown chart in Oversikt tab (per D-08, D-09, D-10):**

Place AFTER the status pie chart / RadialProgressRings section in the Oversikt tab. Wrap in a visibility check: `preferences.modules.find(m => m.id === 'categoryChart')?.visible !== false`.

Chart spec (from UI-SPEC.md):
- `<BarChart layout="vertical" data={summary.byCategory}>`
- `<ResponsiveContainer width="100%" height={Math.max(200, (summary?.byCategory?.length || 0) * 40)}>`
- `<XAxis type="number" allowDecimals={false} tick={{ fontSize: 12 }} />`
- `<YAxis type="category" dataKey="category" tick={{ fontSize: 12 }} width={80} />`
- `<Tooltip contentStyle={{ backgroundColor: 'hsl(var(--card))', border: '1px solid hsl(var(--border))', borderRadius: '8px' }} />`
- `<Bar dataKey="count" name="Arenden" radius={[0,4,4,0]}>` with `<Cell>` per entry using `COLORS[index % COLORS.length]`
- Left margin: `margin={{ left: 80, right: 20, top: 5, bottom: 5 }}`
- Empty state: `<div className="h-[200px] flex items-center justify-center text-muted-foreground">Inga kategorier</div>`

Wrap the chart section in a Card with CardHeader containing `<CardTitle>Kategorier</CardTitle>`.

**C. Replace trend bar chart with ComposedChart (per D-11, D-12):**

In the Trend tab, find the existing `<BarChart>` that shows monthly created tickets and replace with:

- `<ResponsiveContainer width="100%" height={300}>`
- `<ComposedChart data={summary.trend} margin={{ left: 20, right: 20, top: 5, bottom: 5 }}>`
- `<XAxis dataKey="month" tick={{ fontSize: 12 }} />`
- `<YAxis allowDecimals={false} tick={{ fontSize: 12 }} />`
- `<Tooltip contentStyle={{ backgroundColor: 'hsl(var(--card))', border: '1px solid hsl(var(--border))', borderRadius: '8px' }} />`
- `<Legend />`
- `<Bar dataKey="created" name="Skapad" fill="hsl(var(--primary))" radius={[4,4,0,0]} />`
- `<Line type="monotone" dataKey="closed" name="Stangd" stroke="hsl(var(--chart-4))" strokeWidth={2} dot={{ r: 3 }} />`

**D. Register categoryChart module in useReportsPreferences.ts:**

1. Add `'categoryChart'` to the `ReportModuleId` union type.
2. Add to the `DEFAULT_MODULES` array:
```typescript
{
  id: 'categoryChart',
  label: 'Kategorier',
  description: 'Arenden per kategori',
  visible: true,
}
```

IMPORTANT: All text labels must be in Swedish (per CONTEXT.md specifics). Use special characters: "Arenden" should be "Arenden" (with umlaut if the codebase uses them — check existing labels). Chart names: "Skapad", "Stangd". Section heading: "Kategorier". Empty state: "Inga kategorier".
  </action>
  <verify>
    <automated>cd /Users/anton/Library/CloudStorage/OneDrive-Prefabmästarna/Dokument/Projekt/it-system && grep -c "useTickets" src/pages/Reports.tsx && grep -n "useReportsSummary" src/pages/Reports.tsx | head -3 && grep -n "ComposedChart" src/pages/Reports.tsx | head -3 && grep -n "categoryChart" src/hooks/useReportsPreferences.ts | head -3 && grep -n "layout=\"vertical\"" src/pages/Reports.tsx | head -2</automated>
  </verify>
  <acceptance_criteria>
    - src/pages/Reports.tsx does NOT contain `import { useTickets }` or `from '@/hooks/useTickets'` (count = 0)
    - src/pages/Reports.tsx contains `import { useReportsSummary }` or `from '@/hooks/useReportsSummary'`
    - src/pages/Reports.tsx contains `ComposedChart` import from recharts
    - src/pages/Reports.tsx contains `<Line` element with `dataKey="closed"` and `stroke="hsl(var(--chart-4))"`
    - src/pages/Reports.tsx contains `layout="vertical"` (the horizontal category bar chart)
    - src/pages/Reports.tsx contains `COLORS[index % COLORS.length]` in a Cell for category bars
    - src/pages/Reports.tsx contains `Inga kategorier` empty state text
    - src/pages/Reports.tsx contains `Printer` import from lucide-react
    - src/hooks/useReportsPreferences.ts contains `'categoryChart'` in ReportModuleId union
    - src/hooks/useReportsPreferences.ts DEFAULT_MODULES array contains entry with `id: 'categoryChart'`
    - No `useMemo` blocks remain that aggregate from a raw tickets array
  </acceptance_criteria>
  <done>Reports.tsx uses useReportsSummary for all data, category horizontal bar chart visible in Oversikt tab, ComposedChart with created bars + closed line in Trend tab, categoryChart registered in module preferences, useTickets fully removed from Reports.tsx</done>
</task>

</tasks>

<verification>
- Reports.tsx imports useReportsSummary, not useTickets
- Category bar chart renders in Oversikt tab with COLORS array
- Trend tab shows ComposedChart with Bar (created/Skapad) and Line (closed/Stangd)
- categoryChart module registered in useReportsPreferences.ts
- Loading skeleton and error alert states implemented
</verification>

<success_criteria>
- All chart data flows through useReportsSummary (no client-side aggregation on paginated tickets)
- Category breakdown chart is visible showing per-category ticket counts
- Timeline chart shows both created and closed trends overlaid
- Module visibility system respects categoryChart toggle
</success_criteria>

<output>
After completion, create `.planning/phases/01-reports-fix-improvements/01-02-SUMMARY.md`
</output>
