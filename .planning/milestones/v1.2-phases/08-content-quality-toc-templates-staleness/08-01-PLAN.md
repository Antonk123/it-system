---
phase: 08-content-quality-toc-templates-staleness
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - server/src/db/connection.ts
  - server/src/routes/kb.ts
  - src/lib/api.ts
  - src/pages/KnowledgeBase.tsx
  - src/pages/KBArticleDetail.tsx
autonomous: true
requirements: [QUAL-02, QUAL-03]

must_haves:
  truths:
    - "Article detail page shows 'Markera som granskad' button that updates last_reviewed_at timestamp"
    - "KB list shows 'Visa inaktuella' toggle that filters to articles not reviewed in 90+ days"
    - "Stale articles display an amber 'Inaktuell' badge in the KB list"
    - "Never-reviewed articles fall back to created_at for staleness calculation"
  artifacts:
    - path: "server/src/db/connection.ts"
      provides: "ensureKbReviewColumn migration"
      contains: "last_reviewed_at"
    - path: "server/src/routes/kb.ts"
      provides: "PATCH /api/kb/articles/:id/review endpoint"
      contains: "router.patch"
    - path: "src/lib/api.ts"
      provides: "reviewKbArticle method + stale param + last_reviewed_at on KbArticleRow"
      contains: "reviewKbArticle"
    - path: "src/pages/KnowledgeBase.tsx"
      provides: "Stale filter toggle and stale badge"
      contains: "staleFilter"
    - path: "src/pages/KBArticleDetail.tsx"
      provides: "Markera som granskad button"
      contains: "Markera som granskad"
  key_links:
    - from: "src/pages/KBArticleDetail.tsx"
      to: "/api/kb/articles/:id/review"
      via: "api.reviewKbArticle(id)"
      pattern: "reviewKbArticle"
    - from: "src/pages/KnowledgeBase.tsx"
      to: "/api/kb/articles?stale=1"
      via: "api.getKbArticles({ stale: true })"
      pattern: "stale"
    - from: "server/src/routes/kb.ts"
      to: "kb_articles.last_reviewed_at"
      via: "COALESCE(a.last_reviewed_at, a.created_at)"
      pattern: "julianday.*COALESCE"
---

<objective>
Add staleness detection to the Knowledge Base: schema migration for last_reviewed_at, a review endpoint, stale filtering in the article list, stale badge on article cards, and a "Markera som granskad" button on the article detail page.

Purpose: Let users identify articles that have not been reviewed recently (90+ days) so content stays trustworthy and up-to-date (per D-10 through D-14).
Output: Working staleness feature end-to-end — backend migration, API, and frontend UI.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-content-quality-toc-templates-staleness/08-CONTEXT.md
@.planning/phases/08-content-quality-toc-templates-staleness/08-RESEARCH.md

<interfaces>
<!-- From server/src/db/connection.ts — migration pattern -->
const ensureKbV2Columns = () => {
  if (!tableExists('kb_articles')) return;
  if (!columnExists('kb_articles', 'status')) {
    db.exec(`ALTER TABLE kb_articles ADD COLUMN status TEXT ...`);
  }
};
// Called in initializeDatabase() after ensureKbArticleTagsTable() at line 518

<!-- From server/src/routes/kb.ts — route patterns -->
// GET /api/kb/articles uses named params: @search, @category_id, @article_type, @tag
// FTS branch starts line 162, standard branch starts line 180
// DELETE route at line 354 — PATCH review goes before it
// GET single article at line 208

<!-- From src/lib/api.ts — KbArticleRow interface (line 1120) -->
export interface KbArticleRow {
  id: string; title: string; content: string;
  category_id: string | null; category_name: string | null; category_color: string | null;
  article_type?: string | null; status: 'draft' | 'published';
  view_count: number; tags: string[]; snippet?: string | null;
  created_at: string; updated_at: string;
}
// getKbArticles at line 781 — params: { search?, category_id?, article_type?, tag? }

<!-- From src/pages/KnowledgeBase.tsx -->
// hasActiveFilters at line 93
// fetchArticles at line 55 builds params object
// Article card badge cluster at lines 393-412

<!-- From src/pages/KBArticleDetail.tsx -->
// Metadata flex row at lines 213-238 (status, category, dates, view count)
// Action bar at lines 120-168 (4 buttons already)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Backend — migration, PATCH endpoint, stale filter SQL</name>
  <read_first>
    - server/src/db/connection.ts (lines 436-522 for migration pattern and initializeDatabase)
    - server/src/routes/kb.ts (lines 149-233 for GET articles list and GET single article SQL)
  </read_first>
  <files>server/src/db/connection.ts, server/src/routes/kb.ts</files>
  <action>
**connection.ts — Add migration function (after ensureKbArticleTagsTable definition, around line 463):**

Create ensureKbReviewColumn following the same pattern as ensureKbV2Columns:
- Check tableExists('kb_articles'), return if not
- Check columnExists('kb_articles', 'last_reviewed_at'), return if exists
- Run: ALTER TABLE kb_articles ADD COLUMN last_reviewed_at TEXT;
- Console log the addition

Call ensureKbReviewColumn() in initializeDatabase() after ensureKbArticleTagsTable() (line 518).

**kb.ts — Add PATCH review endpoint (before DELETE route at line 354):**

Route: router.patch('/articles/:id/review', authenticate, ...)
- Check article exists with SELECT id FROM kb_articles WHERE id = ?
- Return 404 if not found
- Set last_reviewed_at to new Date().toISOString()
- UPDATE kb_articles SET last_reviewed_at = ? WHERE id = ?
- Return JSON { last_reviewed_at: now }

**kb.ts — Add stale filter to GET /api/kb/articles (per D-13):**

1. At line 154, add stale to destructured query params:
   const { search, category_id, article_type, tag, stale } = req.query as Record<string, string>;

2. In FTS branch SELECT (line 162), add a.last_reviewed_at after a.view_count in the column list. Add WHERE clause after the tag filter (line 175):
   AND (@stale IS NULL OR (julianday('now') - julianday(COALESCE(a.last_reviewed_at, a.created_at))) > 90)
   Add stale: stale || null to the .all() named params object.

3. In standard branch SELECT (line 180), same changes: add a.last_reviewed_at to columns, add the same WHERE clause after tag filter (line 190), add stale: stale || null to .all() params.

4. In GET /api/kb/articles/:id SELECT (line 211), add a.last_reviewed_at after a.view_count in the column list.
  </action>
  <verify>
    <automated>cd /Users/anton/Library/CloudStorage/OneDrive-Prefabmästarna/Dokument/Projekt/it-system && grep -n "ensureKbReviewColumn" server/src/db/connection.ts && grep -n "router.patch.*review" server/src/routes/kb.ts && grep -c "julianday.*COALESCE.*last_reviewed_at" server/src/routes/kb.ts</automated>
  </verify>
  <acceptance_criteria>
    - server/src/db/connection.ts contains "const ensureKbReviewColumn"
    - server/src/db/connection.ts contains "ensureKbReviewColumn()" call inside initializeDatabase
    - server/src/routes/kb.ts contains "router.patch('/articles/:id/review'"
    - server/src/routes/kb.ts contains julianday COALESCE last_reviewed_at in at least 2 places (FTS + standard)
    - server/src/routes/kb.ts GET single article SELECT contains a.last_reviewed_at
  </acceptance_criteria>
  <done>Database migration adds last_reviewed_at column. PATCH endpoint updates it. GET list filters by staleness when stale param is set. GET single article returns last_reviewed_at.</done>
</task>

<task type="auto">
  <name>Task 2: Frontend — API client, stale filter toggle, stale badge, review button</name>
  <read_first>
    - src/lib/api.ts (lines 781-793 for getKbArticles and getKbArticle, lines 1120-1134 for KbArticleRow)
    - src/pages/KnowledgeBase.tsx (full file — filter state, hasActiveFilters, article card rendering)
    - src/pages/KBArticleDetail.tsx (full file — metadata area, action bar)
  </read_first>
  <files>src/lib/api.ts, src/pages/KnowledgeBase.tsx, src/pages/KBArticleDetail.tsx</files>
  <action>
**api.ts — Extend KbArticleRow (line 1120):**
Add field after updated_at: last_reviewed_at?: string | null;

**api.ts — Extend getKbArticles (line 781):**
Add stale?: boolean to the params type object. In method body add:
if (params?.stale) qs.set('stale', '1');

**api.ts — Add reviewKbArticle method (after getKbArticle around line 793):**
```
async reviewKbArticle(id: string) {
  return this.request<{ last_reviewed_at: string }>(`/kb/articles/${id}/review`, {
    method: 'PATCH',
  });
}
```

**KnowledgeBase.tsx — Stale filter and badge (per D-13, D-14):**

1. Add imports: Switch from '@/components/ui/switch', Label from '@/components/ui/label', AlertTriangle to lucide-react import.

2. Add state (after tagFilter): const [staleFilter, setStaleFilter] = useState(false);

3. In fetchArticles (line 55), update params type to include stale?: boolean. After tagFilter check add: if (staleFilter) params.stale = true;
   Add staleFilter to useCallback deps array.

4. Update hasActiveFilters (line 93) to:
   const hasActiveFilters = search || categoryFilter !== 'all' || typeFilter !== 'all' || tagFilter !== 'all' || staleFilter;

5. After the tag filter Select (around line 326), add stale toggle:
```
<div className="flex items-center gap-2 shrink-0">
  <Switch id="stale-filter" checked={staleFilter} onCheckedChange={setStaleFilter} />
  <Label htmlFor="stale-filter" className="text-sm cursor-pointer whitespace-nowrap">Visa inaktuella</Label>
</div>
```

6. Add isStale helper inside component before return:
```
const isStale = (article: KbArticleRow): boolean => {
  const ref = article.last_reviewed_at || article.created_at;
  return (Date.now() - new Date(ref).getTime()) / (86400 * 1000) > 90;
};
```

7. In article card badge cluster (after existing type badge, around line 408), add:
```
{isStale(article) && (
  <Badge variant="outline" className="text-xs border-amber-500/50 text-amber-600 gap-1">
    <AlertTriangle className="w-3 h-3" />
    Inaktuell
  </Badge>
)}
```

**KBArticleDetail.tsx — Review button (per D-11):**

1. Add CheckCircle to lucide-react import.

2. Add state: const [isReviewing, setIsReviewing] = useState(false);

3. Add handler:
```
const handleMarkReviewed = async () => {
  if (!id) return;
  setIsReviewing(true);
  try {
    const result = await api.reviewKbArticle(id);
    setArticle(prev => prev ? { ...prev, last_reviewed_at: result.last_reviewed_at } : prev);
    toast.success('Artikel markerad som granskad');
  } catch {
    toast.error('Kunde inte markera som granskad');
  } finally {
    setIsReviewing(false);
  }
};
```

4. Place review button in metadata area (after view count span at line 237, before closing div). This avoids crowding the 4-button action bar:
```
<Button
  variant="ghost"
  size="sm"
  className="h-auto py-0.5 px-2 text-sm text-muted-foreground hover:text-green-600 gap-1.5"
  onClick={handleMarkReviewed}
  disabled={isReviewing}
>
  <CheckCircle className="w-3.5 h-3.5" />
  {article.last_reviewed_at
    ? `Granskad ${formatDate(article.last_reviewed_at)}`
    : 'Markera som granskad'}
</Button>
```
  </action>
  <verify>
    <automated>cd /Users/anton/Library/CloudStorage/OneDrive-Prefabmästarna/Dokument/Projekt/it-system && grep -n "last_reviewed_at" src/lib/api.ts && grep -n "reviewKbArticle" src/lib/api.ts && grep -c "staleFilter" src/pages/KnowledgeBase.tsx && grep -n "Inaktuell" src/pages/KnowledgeBase.tsx && grep -n "handleMarkReviewed" src/pages/KBArticleDetail.tsx</automated>
  </verify>
  <acceptance_criteria>
    - src/lib/api.ts KbArticleRow contains "last_reviewed_at"
    - src/lib/api.ts contains "async reviewKbArticle(id: string)"
    - src/lib/api.ts getKbArticles params type contains "stale?: boolean"
    - src/pages/KnowledgeBase.tsx contains "const [staleFilter, setStaleFilter] = useState(false)"
    - src/pages/KnowledgeBase.tsx hasActiveFilters line includes "staleFilter"
    - src/pages/KnowledgeBase.tsx contains "Inaktuell" badge text
    - src/pages/KnowledgeBase.tsx contains Switch import and "Visa inaktuella" label
    - src/pages/KBArticleDetail.tsx contains "handleMarkReviewed" function
    - src/pages/KBArticleDetail.tsx contains "Markera som granskad" text
    - src/pages/KBArticleDetail.tsx contains "CheckCircle" in import
  </acceptance_criteria>
  <done>API client extended with reviewKbArticle and stale param. KB list has working stale filter toggle and amber stale badge on old articles. Article detail has review button showing last review date or "Markera som granskad".</done>
</task>

</tasks>

<verification>
1. grep -rn "last_reviewed_at" server/src/ src/lib/api.ts — column in migration, routes, API client
2. grep -n "ensureKbReviewColumn" server/src/db/connection.ts — migration defined and called
3. grep -n "staleFilter" src/pages/KnowledgeBase.tsx — filter state, dependency, hasActiveFilters, UI toggle
4. grep -n "Markera som granskad" src/pages/KBArticleDetail.tsx — review button present
5. Build check: npx tsc --noEmit 2>&1 | head -20
</verification>

<success_criteria>
- PATCH /api/kb/articles/:id/review endpoint exists and updates last_reviewed_at
- GET /api/kb/articles returns last_reviewed_at and supports ?stale=1 filter
- GET /api/kb/articles/:id returns last_reviewed_at
- KB list page shows "Visa inaktuella" Switch toggle
- Stale articles (90+ days) show amber "Inaktuell" badge in KB list
- staleFilter included in hasActiveFilters (hides "Senast uppdaterade" when active)
- Article detail shows "Markera som granskad" button near dates
- After clicking review, button shows "Granskad {date}"
- TypeScript compiles without errors
</success_criteria>

<output>
After completion, create `.planning/phases/08-content-quality-toc-templates-staleness/08-01-SUMMARY.md`
</output>
