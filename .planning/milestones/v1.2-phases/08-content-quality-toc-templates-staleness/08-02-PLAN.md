---
phase: 08-content-quality-toc-templates-staleness
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/pages/KBArticleDetail.tsx
  - src/pages/KBArticleForm.tsx
autonomous: true
requirements: [TOC-01, TOC-02, TMPL-01, TMPL-02]

must_haves:
  truths:
    - "Article detail page shows a table of contents sidebar on desktop when article has 2+ headings"
    - "ToC is a collapsible section above content on mobile"
    - "Clicking a ToC entry scrolls to the corresponding heading via anchor link"
    - "ToC is hidden when article has fewer than 2 headings"
    - "New article form shows a template picker with 3 template cards"
    - "Selecting a template fills the Tiptap editor with predefined Swedish-language structure"
    - "Template picker can be dismissed to start with blank article"
    - "Template picker is not shown when editing an existing article"
  artifacts:
    - path: "src/pages/KBArticleDetail.tsx"
      provides: "ToC sidebar with anchor links and scroll-spy"
      contains: "tocItems"
    - path: "src/pages/KBArticleForm.tsx"
      provides: "Template picker with 3 hardcoded templates"
      contains: "ARTICLE_TEMPLATES"
  key_links:
    - from: "src/pages/KBArticleDetail.tsx ToC links"
      to: "heading elements in rendered HTML"
      via: "href=#slug pointing to id set by useEffect DOM mutation"
      pattern: "el.setAttribute.*id.*slug"
    - from: "src/pages/KBArticleForm.tsx template picker"
      to: "RichTextEditor value prop"
      via: "setContent(tmpl.body) updating state that RichTextEditor syncs"
      pattern: "setContent.*tmpl.body"
---

<objective>
Add table of contents with anchor links to article detail page, and a template picker for new article creation. Both are pure frontend features with no backend changes.

Purpose: ToC makes long articles navigable (per D-01 through D-04). Templates ensure consistent article structure (per D-05 through D-09).
Output: ToC sidebar/collapsible on article detail, template card picker on new article form.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-content-quality-toc-templates-staleness/08-CONTEXT.md
@.planning/phases/08-content-quality-toc-templates-staleness/08-RESEARCH.md

<interfaces>
<!-- From src/pages/KBArticleDetail.tsx — current layout structure -->
// Outer: <div className="max-w-3xl mx-auto p-4 md:p-6 space-y-6">
// Action bar (lines 120-168): Back button + 4 action buttons
// Share panel (lines 172-208)
// Article header (lines 211-246): title, metadata, tags
// Content (lines 249-255): <div className="prose-wrapper ..."><HtmlRenderer content={article.content} /></div>
// Linked tickets (lines 258-281)

<!-- CRITICAL: DOMPurify strips id attributes -->
// HtmlRenderer ALLOWED_ATTR: ['href', 'target', 'rel', 'class', 'style', 'colspan', 'rowspan', 'align']
// id is NOT allowed — must use post-render DOM mutation via useRef + querySelectorAll

<!-- From src/pages/KBArticleForm.tsx — form structure -->
// isEditing = Boolean(id) at line 17
// const [content, setContent] = useState('') at line 20
// RichTextEditor at line 229: <RichTextEditor value={content} onChange={setContent} />
// RichTextEditor syncs value prop via internal useEffect — setContent(html) works

<!-- From src/components/HtmlRenderer.tsx -->
// Renders sanitized HTML, allows h1-h6 in ALLOWED_TAGS
// ALLOWED_ATTR does NOT include 'id' — IDs must be set post-render

<!-- cn utility available from @/lib/utils -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Table of Contents on article detail page</name>
  <read_first>
    - src/pages/KBArticleDetail.tsx (full file — understand layout, imports, state)
    - src/components/HtmlRenderer.tsx (verify ALLOWED_ATTR excludes id)
  </read_first>
  <files>src/pages/KBArticleDetail.tsx</files>
  <action>
**Add imports:** Add useRef to the React import. Add cn from '@/lib/utils'.

**Add TocItem type and state (inside component, after existing state declarations):**
```
type TocItem = { id: string; text: string; level: number };
```
(Declare above the component or inside it.)

Add state and ref:
```
const contentRef = useRef<HTMLDivElement>(null);
const [tocItems, setTocItems] = useState<TocItem[]>([]);
const [activeId, setActiveId] = useState<string>('');
```

**Add slugify helper (inside or outside component):**
```
const slugify = (text: string): string =>
  text
    .toLowerCase()
    .replace(/[åä]/g, 'a')
    .replace(/[ö]/g, 'o')
    .replace(/[^a-z0-9]+/g, '-')
    .replace(/^-|-$/g, '');
```

**Add heading extraction useEffect (after existing useEffects):**
```
useEffect(() => {
  if (!contentRef.current || !article?.content) return;
  const headings = contentRef.current.querySelectorAll('h1, h2, h3, h4, h5, h6');
  const usedSlugs = new Set<string>();
  const items: TocItem[] = [];
  headings.forEach((el) => {
    const text = el.textContent?.trim() ?? '';
    if (!text) return;
    let slug = slugify(text);
    if (usedSlugs.has(slug)) {
      let i = 2;
      while (usedSlugs.has(`${slug}-${i}`)) i++;
      slug = `${slug}-${i}`;
    }
    usedSlugs.add(slug);
    el.setAttribute('id', slug);
    items.push({ id: slug, text, level: parseInt(el.tagName[1]) });
  });
  setTocItems(items);
}, [article?.content]);
```

**Add IntersectionObserver scroll-spy useEffect (Claude's discretion — implement it):**
```
useEffect(() => {
  if (tocItems.length < 2 || !contentRef.current) return;
  const observer = new IntersectionObserver(
    (entries) => {
      const visible = entries.filter(e => e.isIntersecting);
      if (visible.length > 0) setActiveId(visible[0].target.id);
    },
    { rootMargin: '-10% 0% -60% 0%', threshold: 0 }
  );
  const headings = contentRef.current.querySelectorAll('h1, h2, h3, h4, h5, h6');
  headings.forEach((el) => observer.observe(el));
  return () => observer.disconnect();
}, [tocItems]);
```

**Restructure layout for ToC sidebar (per D-01):**

Change outer container from max-w-3xl to max-w-4xl:
`<div className="max-w-4xl mx-auto p-4 md:p-6 space-y-6">`

Keep action bar, share panel, and article header wrapped in max-w-3xl:
```
<div className="max-w-3xl">
  {/* action bar */}
  {/* share panel */}
  {/* article header with metadata */}
</div>
```

Replace the current content div and linked tickets with a flex layout:
```
{/* Content + ToC side-by-side */}
<div className="flex gap-8 items-start">
  {/* Main content column */}
  <div className="flex-1 min-w-0">
    {/* Mobile ToC — collapsible, per D-01 */}
    {tocItems.length >= 2 && (
      <details className="lg:hidden mb-4 border rounded-lg p-3 bg-card print:hidden">
        <summary className="text-sm font-medium cursor-pointer select-none">
          Innehall
        </summary>
        <nav className="mt-2 space-y-1">
          {tocItems.map((item) => (
            <a key={item.id} href={`#${item.id}`}
              className={cn(
                'block text-sm py-0.5 transition-colors',
                item.level >= 3 ? 'pl-4' : item.level === 2 ? 'pl-2' : '',
                'text-muted-foreground hover:text-foreground'
              )}>
              {item.text}
            </a>
          ))}
        </nav>
      </details>
    )}
    {/* Article content — add ref */}
    <div ref={contentRef} className="prose-wrapper border border-border rounded-lg p-5 bg-card min-h-[200px]">
      {article.content ? (
        <HtmlRenderer content={article.content} />
      ) : (
        <p className="text-muted-foreground text-sm italic">Inget innehall annu.</p>
      )}
    </div>
  </div>

  {/* Desktop ToC sidebar — per D-01 sticky right side */}
  {tocItems.length >= 2 && (
    <aside className="hidden lg:block w-52 shrink-0 print:hidden">
      <div className="sticky top-24 space-y-1">
        <p className="text-xs font-semibold uppercase tracking-wider text-muted-foreground mb-3">
          Innehall
        </p>
        {tocItems.map((item) => (
          <a
            key={item.id}
            href={`#${item.id}`}
            className={cn(
              'block text-sm py-0.5 transition-colors',
              item.level >= 3 ? 'pl-4' : item.level === 2 ? 'pl-2' : '',
              activeId === item.id
                ? 'text-primary font-medium'
                : 'text-muted-foreground hover:text-foreground'
            )}
          >
            {item.text}
          </a>
        ))}
      </div>
    </aside>
  )}
</div>

{/* Linked tickets — constrained width */}
<div className="max-w-3xl pt-2 border-t">
  {/* existing linked tickets content unchanged */}
</div>
```

Per D-04, the ToC (both mobile and desktop) only renders when tocItems.length >= 2.
Per Pitfall 5, both ToC elements have print:hidden class.
Per Pitfall 6, only the content+ToC flex row uses full width; header and linked tickets stay at max-w-3xl.
  </action>
  <verify>
    <automated>cd /Users/anton/Library/CloudStorage/OneDrive-Prefabmästarna/Dokument/Projekt/it-system && grep -n "tocItems" src/pages/KBArticleDetail.tsx | head -5 && grep -n "contentRef" src/pages/KBArticleDetail.tsx | head -3 && grep -n "slugify" src/pages/KBArticleDetail.tsx && grep -n "IntersectionObserver" src/pages/KBArticleDetail.tsx && grep -n "print:hidden" src/pages/KBArticleDetail.tsx && grep -n "max-w-4xl" src/pages/KBArticleDetail.tsx</automated>
  </verify>
  <acceptance_criteria>
    - KBArticleDetail.tsx contains "const contentRef = useRef"
    - KBArticleDetail.tsx contains "const [tocItems, setTocItems] = useState"
    - KBArticleDetail.tsx contains "const slugify" function
    - KBArticleDetail.tsx contains "el.setAttribute('id', slug)" (post-render DOM mutation)
    - KBArticleDetail.tsx contains "tocItems.length >= 2" guard (at least 2 occurrences: mobile + desktop)
    - KBArticleDetail.tsx contains "IntersectionObserver" for scroll-spy
    - KBArticleDetail.tsx contains "print:hidden" on ToC elements (at least 2 occurrences)
    - KBArticleDetail.tsx outer container uses "max-w-4xl" (not max-w-3xl)
    - KBArticleDetail.tsx contains "sticky top-24" for desktop ToC
    - KBArticleDetail.tsx contains href={`#${item.id}`} anchor links
  </acceptance_criteria>
  <done>Article detail page shows sticky ToC sidebar on desktop (right side) and collapsible section on mobile. ToC generated from rendered headings with slugified anchor IDs. Scroll-spy highlights active heading. ToC hidden when fewer than 2 headings. ToC hidden in print.</done>
</task>

<task type="auto">
  <name>Task 2: Article template picker on new article form</name>
  <read_first>
    - src/pages/KBArticleForm.tsx (full file — understand isEditing, content state, form structure)
    - src/components/ui/rich-text-editor.tsx (verify value prop sync behavior)
  </read_first>
  <files>src/pages/KBArticleForm.tsx</files>
  <action>
**Add ARTICLE_TEMPLATES constant (outside the component, at top of file after imports):**

Per D-07, three templates with proper Swedish characters (a, a, o):
```
const ARTICLE_TEMPLATES = [
  {
    id: 'solution',
    label: 'Losning',
    description: 'Problem, orsak, losning, forebyggande',
    body: '<h2>Problem</h2><p>Beskriv problemet som artikeln loser.</p><h2>Orsak</h2><p>Varfor uppstar problemet?</p><h2>Losning</h2><p>Steg-for-steg losning.</p><h2>Forebyggande</h2><p>Hur forhindrar man att problemet aterkommer?</p>',
  },
  {
    id: 'how-to',
    label: 'Instruktion',
    description: 'Forutsattningar, steg, verifiering',
    body: '<h2>Forutsattningar</h2><p>Vad behovs innan du borjar?</p><h2>Steg</h2><ol><li>Steg ett</li><li>Steg tva</li><li>Steg tre</li></ol><h2>Verifiering</h2><p>Hur vet du att det fungerade?</p>',
  },
  {
    id: 'troubleshooting',
    label: 'Felsokning',
    description: 'Symptom, diagnos, atgard',
    body: '<h2>Symptom</h2><p>Vad ser anvandaren?</p><h2>Diagnos</h2><p>Hur identifierar du grundorsaken?</p><h2>Atgard</h2><p>Vilka atgarder loser problemet?</p>',
  },
] as const;
```

IMPORTANT: The template body strings MUST use proper Swedish characters. The examples above show ASCII-only to avoid encoding issues in this plan file. The actual implementation must use: Losning -> Losning (with o-umlaut), Forutsattningar -> Forutsattningar (with o-umlaut), etc. Specifically:
- Losning -> L\u00f6sning
- Orsak stays Orsak
- losning -> l\u00f6sning
- Forebyggande -> F\u00f6rebyggande
- forhindrar -> f\u00f6rhindrar
- aterkommer -> \u00e5terkommer
- Forutsattningar -> F\u00f6ruts\u00e4ttningar
- behovs -> beh\u00f6vs
- borjar -> b\u00f6rjar
- Felsokning -> Fels\u00f6kning
- Atgard -> \u00c5tg\u00e4rd
- anvandaren -> anv\u00e4ndaren
- atgarder -> \u00e5tg\u00e4rder
- loser -> l\u00f6ser
- uppstar -> uppst\u00e5r
- Varfor -> Varf\u00f6r

**Add template dismissed state (inside component, after existing state):**
```
const [templateDismissed, setTemplateDismissed] = useState(false);
```

**Add template picker UI (per D-05, D-06, D-08):**
Place BEFORE the form element (line 145), after the header div (line 142). Only show when creating new article and not dismissed:

```
{!isEditing && !templateDismissed && (
  <div className="space-y-3 p-4 rounded-lg border border-border bg-muted/20">
    <div className="flex items-center justify-between">
      <p className="text-sm font-medium">Valj mall (valfritt)</p>
      <button type="button" onClick={() => setTemplateDismissed(true)}
        className="text-xs text-muted-foreground hover:text-foreground">
        Hoppa over
      </button>
    </div>
    <div className="grid grid-cols-1 sm:grid-cols-3 gap-3">
      {ARTICLE_TEMPLATES.map((tmpl) => (
        <button key={tmpl.id} type="button"
          onClick={() => { setContent(tmpl.body); setTemplateDismissed(true); }}
          className="text-left p-4 rounded-lg border border-border hover:border-primary/50 hover:bg-accent transition-colors">
          <p className="font-medium text-sm">{tmpl.label}</p>
          <p className="text-xs text-muted-foreground mt-1">{tmpl.description}</p>
        </button>
      ))}
    </div>
  </div>
)}
```

Note on "Valj mall" — must use proper Swedish: V\u00e4lj mall (valfritt). And "Hoppa over" -> Hoppa \u00f6ver.

Per D-06, clicking a template calls setContent(tmpl.body) which updates the content state. The RichTextEditor component picks up the new value via its internal useEffect that syncs value prop to the editor. Per D-08, the "Hoppa over" button dismisses the picker.
  </action>
  <verify>
    <automated>cd /Users/anton/Library/CloudStorage/OneDrive-Prefabmästarna/Dokument/Projekt/it-system && grep -n "ARTICLE_TEMPLATES" src/pages/KBArticleForm.tsx && grep -n "templateDismissed" src/pages/KBArticleForm.tsx && grep -c "tmpl.body\|tmpl.label" src/pages/KBArticleForm.tsx && grep -n "isEditing.*templateDismissed" src/pages/KBArticleForm.tsx</automated>
  </verify>
  <acceptance_criteria>
    - KBArticleForm.tsx contains "const ARTICLE_TEMPLATES" array with 3 entries
    - KBArticleForm.tsx ARTICLE_TEMPLATES contains objects with id 'solution', 'how-to', 'troubleshooting'
    - KBArticleForm.tsx contains "const [templateDismissed, setTemplateDismissed] = useState(false)"
    - KBArticleForm.tsx contains "!isEditing && !templateDismissed" guard
    - KBArticleForm.tsx contains "setContent(tmpl.body)" on template click
    - KBArticleForm.tsx contains "setTemplateDismissed(true)" on both template select and dismiss
    - KBArticleForm.tsx template labels include "Instruktion" and "Felsokning" (with proper Swedish chars)
    - KBArticleForm.tsx contains "Hoppa" (dismiss button text)
  </acceptance_criteria>
  <done>New article form shows 3 template cards (Solution, How-to, Troubleshooting) with Swedish labels. Selecting a template fills Tiptap editor. Picker can be dismissed. Picker not shown when editing existing article.</done>
</task>

</tasks>

<verification>
1. grep -n "tocItems\|slugify\|contentRef" src/pages/KBArticleDetail.tsx — ToC components present
2. grep -n "ARTICLE_TEMPLATES\|templateDismissed" src/pages/KBArticleForm.tsx — template picker present
3. grep -c "print:hidden" src/pages/KBArticleDetail.tsx — ToC hidden in print (2+ occurrences)
4. Build check: npx tsc --noEmit 2>&1 | head -20
</verification>

<success_criteria>
- Article detail has sticky ToC sidebar on desktop (right side, visible at lg breakpoint)
- Article detail has collapsible ToC on mobile (details/summary element)
- ToC anchor links scroll to headings via href="#slug"
- Heading IDs set via post-render DOM mutation (not pre-sanitization)
- ToC hidden when article has fewer than 2 headings (per D-04)
- ToC hidden in print output (print:hidden)
- Scroll-spy highlights active heading in desktop ToC
- New article form shows 3 template cards with Swedish labels
- Selecting template fills editor with structured HTML
- Template picker dismissible via "Hoppa over" button (per D-08)
- Template picker not shown when editing existing article (per D-05)
- TypeScript compiles without errors
</success_criteria>

<output>
After completion, create `.planning/phases/08-content-quality-toc-templates-staleness/08-02-SUMMARY.md`
</output>
