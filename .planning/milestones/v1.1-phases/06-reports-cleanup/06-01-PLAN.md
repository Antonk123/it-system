---
phase: 06-reports-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/pages/Reports.tsx
  - src/components/ReportsCustomization.tsx
  - src/hooks/useReportsPreferences.ts
  - src/components/ActivityHeatmap.tsx
  - src/components/RadialProgressRings.tsx
autonomous: true
requirements: [RPT-01, RPT-03, RPT-04]

must_haves:
  truths:
    - "Activity Heatmap section is not rendered on the Reports page"
    - "Radial Progress Rings section is not rendered on the Reports page"
    - "The ReportsCustomization gear/settings UI is absent from the Reports header"
    - "All remaining modules are always visible — no isModuleVisible gating"
    - "The Reports layout has no overlapping or duplicated modules"
  artifacts:
    - path: "src/pages/Reports.tsx"
      provides: "Clean Reports page without removed modules or customization"
      contains: "export default Reports"
  key_links:
    - from: "src/pages/Reports.tsx"
      to: "removed components"
      via: "imports removed"
      pattern: "no ActivityHeatmap|RadialProgressRings|ReportsCustomization imports"
---

<objective>
Remove redundant modules (Activity Heatmap, Radial Progress Rings) and the show/hide customization system from the Reports page.

Purpose: Simplify Reports to a clean, focused analytics page with no module toggling overhead and no visual clutter from low-value modules.
Output: A streamlined Reports.tsx with all remaining modules always visible.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/pages/Reports.tsx
@src/hooks/useReportsPreferences.ts
@src/components/ReportsCustomization.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove ActivityHeatmap, RadialProgressRings, and ReportsCustomization from Reports.tsx</name>
  <files>src/pages/Reports.tsx</files>
  <read_first>
    - src/pages/Reports.tsx
    - src/hooks/useReportsPreferences.ts
  </read_first>
  <action>
    In src/pages/Reports.tsx, make these changes:

    1. **Remove imports** for these three components:
       - Delete: `import { ActivityHeatmap } from '@/components/ActivityHeatmap';`
       - Delete: `import { RadialProgressRings } from '@/components/RadialProgressRings';`
       - Delete: `import { ReportsCustomization } from '@/components/ReportsCustomization';`
       - Delete: `import { useReportsPreferences } from '@/hooks/useReportsPreferences';`

    2. **Remove the `useReportsPreferences` hook call and `isModuleVisible` helper:**
       - Delete: `const { preferences } = useReportsPreferences();`
       - Delete the entire `isModuleVisible` function (lines ~162-164)

    3. **Remove all `isModuleVisible(...)` conditional wrappers** — unwrap each module so it renders unconditionally:
       - `isModuleVisible('statusDistribution')` wrapper around the Status Distribution Card (line ~690) — keep the Card, remove the conditional
       - `isModuleVisible('priorityChart')` wrapper around the Priority Chart Card (line ~774) — keep the Card, remove the conditional
       - `isModuleVisible('categoryChart')` wrapper around the Category Chart Card (line ~823) — keep the Card, remove the conditional
       - `isModuleVisible('monthlyChart')` wrapper around the Monthly Trend Card (line ~869) — keep the Card, remove the conditional
       - `isModuleVisible('statusFlow')` wrapper around the Status Flow Card (line ~937) — keep the Card, remove the conditional
       - `isModuleVisible('requesterAnalytics')` wrapper around the Requester Analytics Card (line ~954) — keep the Card, remove the conditional
       - `isModuleVisible('tagAnalytics')` wrapper around the TagAnalytics section (line ~1072) — keep the section, remove the conditional

    4. **Remove the Radial Progress Rings section entirely** (lines ~904-919 in the Trend tab):
       The entire Card wrapping `<RadialProgressRings ... />` under the comment `{/* Radial Progress Rings */}` must be deleted.

    5. **Remove the Activity Heatmap section entirely** (lines ~921-934 in the Trend tab):
       The entire Card wrapping `<ActivityHeatmap ... />` under the comment `{/* Activity Heatmap */}` must be deleted.

    6. **Remove the ReportsCustomization component** from the filter bar (line ~592):
       - Delete `<ReportsCustomization />` from the reports-filter-bar div
       - Also delete the divider `<div className="w-px h-6 bg-border" />` immediately before it (line ~591)

    7. **Clean up unused imports** — after the above removals, check whether these are still used and remove if not:
       - `PieChartIcon` from lucide-react (was used by Radial Progress Rings Card header — check if Status Distribution also uses it; it does at line ~693, so KEEP it)
  </action>
  <verify>
    <automated>cd C:/Users/Anton/Documents/Projekt/it-system && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "ActivityHeatmap" src/pages/Reports.tsx returns 0
    - grep -c "RadialProgressRings" src/pages/Reports.tsx returns 0
    - grep -c "ReportsCustomization" src/pages/Reports.tsx returns 0
    - grep -c "useReportsPreferences" src/pages/Reports.tsx returns 0
    - grep -c "isModuleVisible" src/pages/Reports.tsx returns 0
    - grep -c "export default Reports" src/pages/Reports.tsx returns 1
    - TypeScript compilation succeeds with no errors
  </acceptance_criteria>
  <done>Reports.tsx has no references to ActivityHeatmap, RadialProgressRings, ReportsCustomization, useReportsPreferences, or isModuleVisible. All remaining modules render unconditionally. TypeScript compiles cleanly.</done>
</task>

<task type="auto">
  <name>Task 2: Delete orphaned component and hook files</name>
  <files>
    src/components/ActivityHeatmap.tsx
    src/components/RadialProgressRings.tsx
    src/components/ReportsCustomization.tsx
    src/hooks/useReportsPreferences.ts
  </files>
  <read_first>
    - src/components/ActivityHeatmap.tsx
    - src/components/RadialProgressRings.tsx
    - src/components/ReportsCustomization.tsx
    - src/hooks/useReportsPreferences.ts
  </read_first>
  <action>
    1. Before deleting, verify no other file imports these components. Run grep across the codebase for each:
       - `grep -r "ActivityHeatmap" src/ --include="*.tsx" --include="*.ts"` — should only match the component file itself (Reports.tsx import was removed in Task 1)
       - `grep -r "RadialProgressRings" src/ --include="*.tsx" --include="*.ts"` — same check
       - `grep -r "ReportsCustomization" src/ --include="*.tsx" --include="*.ts"` — same check
       - `grep -r "useReportsPreferences" src/ --include="*.tsx" --include="*.ts"` — same check

    2. If each component/hook is only referenced in its own file (no other consumers), delete the files:
       - `rm src/components/ActivityHeatmap.tsx`
       - `rm src/components/RadialProgressRings.tsx`
       - `rm src/components/ReportsCustomization.tsx`
       - `rm src/hooks/useReportsPreferences.ts`

    3. If any file IS imported elsewhere, do NOT delete it — leave it and note the dependency.

    4. Run TypeScript compilation to confirm no broken imports remain.
  </action>
  <verify>
    <automated>cd C:/Users/Anton/Documents/Projekt/it-system && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - File src/components/ActivityHeatmap.tsx does not exist
    - File src/components/RadialProgressRings.tsx does not exist
    - File src/components/ReportsCustomization.tsx does not exist
    - File src/hooks/useReportsPreferences.ts does not exist
    - TypeScript compilation succeeds with no errors
  </acceptance_criteria>
  <done>All four orphaned files are deleted. No broken imports anywhere in the codebase. TypeScript compiles cleanly.</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes with zero errors
2. `grep -r "ActivityHeatmap\|RadialProgressRings\|ReportsCustomization\|useReportsPreferences\|isModuleVisible" src/ --include="*.tsx" --include="*.ts"` returns no matches
3. The Reports page renders all remaining modules (Status Distribution, Priority Chart, Category Chart, Monthly Trend, Status Flow, Requester Analytics, Tag Analytics) without any show/hide toggle UI
</verification>

<success_criteria>
- ActivityHeatmap and RadialProgressRings are fully removed (components deleted, no imports)
- ReportsCustomization UI and useReportsPreferences hook are fully removed
- All remaining modules render unconditionally (no isModuleVisible checks)
- TypeScript compiles cleanly
- No orphaned files remain
</success_criteria>

<output>
After completion, create `.planning/phases/06-reports-cleanup/06-01-SUMMARY.md`
</output>
