---
phase: 4
slug: filter-consolidation-archive-parity
status: draft
shadcn_initialized: true
preset: "style=default baseColor=slate cssVariables=true"
created: 2026-03-26
---

# Phase 4 — UI Design Contract

> Visual and interaction contract for frontend phases. Generated by gsd-ui-researcher, verified by gsd-ui-checker.

---

## Design System

| Property | Value |
|----------|-------|
| Tool | shadcn |
| Preset | style=default, baseColor=slate, cssVariables=true |
| Component library | Radix UI (via shadcn) |
| Icon library | lucide-react (already in use) |
| Font | Plus Jakarta Sans (already loaded via Google Fonts) |

Source: `components.json`, `tailwind.config.ts`, `src/index.css`

---

## Spacing Scale

Declared values (must be multiples of 4):

| Token | Value | Usage |
|-------|-------|-------|
| xs | 4px | Icon gaps, chip internal padding, separator gaps |
| sm | 8px | Gap between filter controls in the unified row |
| md | 16px | Filter row horizontal padding, chip row padding |
| lg | 24px | Section spacing between filter row and results table |
| xl | 32px | Page content padding (top/sides) |
| 2xl | 48px | Major layout breaks |
| 3xl | 64px | Page-level spacing (not used in this phase) |

Exceptions: Floating bulk action bar uses 12px vertical padding (not on standard scale) to keep it compact and visually distinct from the page content it floats above.

---

## Typography

| Role | Size | Weight | Line Height |
|------|------|--------|-------------|
| Body | 14px | 400 | 1.5 |
| Label | 12px | 500 | 1.4 |
| Heading | 20px | 600 | 1.2 |
| Filter chip text | 12px | 500 | 1 |

Font: Plus Jakarta Sans in all roles.
Source: `src/index.css` (`--font-sans`, existing component usage patterns).

---

## Color

| Role | Value | Usage |
|------|-------|-------|
| Dominant (60%) | hsl(222 47% 6%) — `--background` | Page background, main content area |
| Secondary (30%) | hsl(220 40% 9%) — `--card` | Filter row container, table rows, popovers |
| Accent (10%) | hsl(214 100% 60%) — `--primary` | Active filter chips border + dot indicator, selected row highlight, floating action bar primary button |
| Destructive | hsl(0 84% 60%) — `--destructive` | "Radera permanent" bulk action button and its confirmation dialog |

Accent reserved for: active filter chip border and left-dot indicator, row selection highlight in bulk mode, primary button in the floating bulk action bar. Not used on passive filter controls or table headers.

Secondary semantic: `--success` (hsl 142 71% 45%) — used on the "Öppna igen" (re-open) status badge in the bulk action bar to signal a positive restore action.

Source: `src/index.css` (CSS variable definitions, theme-default block).

---

## Component Inventory

Components to build or extend for this phase:

| Component | Status | Notes |
|-----------|--------|-------|
| `UnifiedFilterBar` | New | Single row: search + status + priority + category + tags + checklist + date popover + views selector. Accepts props: `filterState`, `onChange`, `hideStatus` (for Archive). |
| `ActiveFilterChips` | New (replaces TagFilter/CategoryFilter) | Renders all active filters as uniform chips below the filter row. Single "Rensa alla" at end. Chip anatomy: `[label]: [value] [×]`. |
| `DateRangePopover` | New | Button labeled "Datum" with Calendar icon. Opens Popover containing two Calendar pickers (from/to) and a field selector (created_at / updated_at / closed_at). Field selector hidden on Archive. |
| `BulkActionBar` | New | Fixed-position bar at viewport bottom. Visible only when 1+ rows selected. Shows selected count + 4 action buttons. Slides in with `translate-y` transition (200ms ease-out). |
| `FilterViewSelector` | Extend | Add Archive page awareness — apply preset, silently drop incompatible filters (status). No visual change to the component itself. |
| `useFilterViews` hook | Extend | Support Archive page context so presets are shared across pages. |

Existing components reused directly (no changes):
- `StatusMultiSelect` — Popover+Command multi-select, used inside `UnifiedFilterBar`
- `TagMultiSelect` — Popover+Command multi-select, used inside `UnifiedFilterBar`
- `SearchBar` — Used as-is inside `UnifiedFilterBar`
- `Badge` (`src/components/ui/badge.tsx`) — Base for `ActiveFilterChips`
- `Popover`, `Calendar`, `Button`, `Select`, `Checkbox`, `AlertDialog` — shadcn primitives

---

## Interaction Contracts

### UnifiedFilterBar

- All controls in one horizontal row, wrapping to two lines at `sm` breakpoint (640px).
- Row height: 40px per control (`h-10` Tailwind class, consistent with existing Button/Select usage).
- Filter controls order (left to right): Search (flex-1) | Status | Priority | Category | Tags | Checklist | Datum | Views selector.
- On `sm` and below: Search takes full width on line 1; remaining controls wrap to line 2 as a scrollable horizontal strip.
- Status control is hidden when `hideStatus={true}` (Archive usage). The remaining controls shift left to fill the gap.

### ActiveFilterChips

- Appears as a row directly below the filter control row, only when at least one filter is active.
- Chip anatomy: `[Filter label]: [value] ×` — e.g., "Prioritet: Hög ×".
- All chips share identical visual style: `secondary` background, 1px `primary` border, 12px label at weight 500, `×` remove button with hover state `destructive` text color.
- Date range chip format: "Datum: 2026-01-01 → 2026-03-31 ×".
- "Rensa alla" button at the far right of the chip row: `ghost` variant, 12px text, only visible when 2+ chips are active.
- Chip row animates in with `fade-in` (0.2s ease-out) when first chip appears.
- Removing the last chip hides the chip row with a 150ms opacity fade-out.

### DateRangePopover

- Trigger button: "Datum" label + Calendar icon, `outline` variant, `h-10`.
- When a date range is active, button shows a `primary`-colored dot indicator to the left of the label (same pattern as active state for other filter buttons).
- Popover width: 360px fixed. Contains two stacked date pickers (from / to) with labels "Från" and "Till".
- Date field selector (visible on TicketList only): three radio-style buttons — "Skapad", "Uppdaterad", "Stängd". Default: "Skapad".
- Popover opens with `scale-in` animation (0.2s ease-out, existing keyframe in tailwind config).
- "Rensa datum" text button at bottom of popover, `ghost` variant, only visible when at least one date is selected.

### BulkActionBar (Archive page)

- Triggered by: any row checkbox checked, or header "select all" checkbox.
- Fixed position: `fixed bottom-6 left-1/2 -translate-x-1/2` — centered floating bar above page bottom.
- Visual: `card` background, `shadow-lg`, `rounded-lg`, 12px vertical / 16px horizontal padding.
- Content left: "{N} ärenden valda" label at 14px weight 500.
- Content right: 4 action buttons (left to right):
  1. "Öppna igen" — `outline` variant, success-tinted border (restore to open status)
  2. "Ändra prioritet" — `outline` variant, neutral
  3. "Exportera CSV" — `outline` variant, neutral
  4. "Radera permanent" — `destructive` variant
- Bar slides in with `translateY(100%) → translateY(0)` transition, 200ms ease-out.
- Bar slides out on deselect with `translateY(0) → translateY(100%)`, 150ms ease-in.
- "Radera permanent" opens AlertDialog: title "Radera permanent?", body "Du håller på att radera {N} ärende(n) permanent. Åtgärden kan inte ångras.", cancel "Avbryt", confirm "Radera" (`destructive` button).

### Row Selection (Archive table)

- Checkbox column is the first column, 40px fixed width.
- Header checkbox: unchecked = no selection, indeterminate = partial, checked = all on current page selected.
- Selected rows get `bg-primary/5` background (5% opacity of primary color) as a tinted row highlight.
- Checkbox uses shadcn `Checkbox` component.

---

## Copywriting Contract

| Element | Copy (Swedish — interface language) |
|---------|--------------------------------------|
| Primary CTA (filter row views) | "Spara vy" |
| Filter clear all button | "Rensa alla" |
| Date popover clear button | "Rensa datum" |
| Date field options | "Skapad" / "Uppdaterad" / "Stängd" |
| Checklist filter label | "Med checklista" |
| Bulk action: re-open | "Öppna igen" |
| Bulk action: change priority | "Ändra prioritet" |
| Bulk action: export | "Exportera CSV" |
| Bulk action: delete | "Radera permanent" |
| Bulk bar selection label | "{N} ärende(n) valda" |
| Delete confirmation title | "Radera permanent?" |
| Delete confirmation body | "Du håller på att radera {N} ärende(n) permanent. Åtgärden kan inte ångras." |
| Delete confirmation cancel | "Avbryt" |
| Delete confirmation confirm | "Radera" |
| Archive search placeholder | "Sök arkiverade ärenden..." |
| TicketList search placeholder | "Sök ärenden..." |
| Empty state heading (no filter results) | "Inga ärenden matchar filtret" |
| Empty state body | "Prova att justera dina filterval eller rensa alla filter." |
| Empty state CTA | "Rensa filter" |
| Preset applied with ignored filters | Silent (no toast) — incompatible filters dropped without notification |
| Preset saved toast | "Filtervyn sparades" |
| Preset deleted toast | "Filtervyn togs bort" |
| Export success toast | "{N} ärenden exporterade till CSV" |

Source: existing Swedish-language interface convention (TicketList.tsx, Archive.tsx). All copy is Swedish to match the established interface language.

---

## Registry Safety

| Registry | Blocks Used | Safety Gate |
|----------|-------------|-------------|
| shadcn official | Button, Popover, Calendar, Select, Command, Checkbox, AlertDialog, Badge | not required |
| Third-party | none | not applicable |

No third-party registries are used. All components are either shadcn official primitives or custom-built from scratch using existing project patterns.

---

## Checker Sign-Off

- [ ] Dimension 1 Copywriting: PASS
- [ ] Dimension 2 Visuals: PASS
- [ ] Dimension 3 Color: PASS
- [ ] Dimension 4 Typography: PASS
- [ ] Dimension 5 Spacing: PASS
- [ ] Dimension 6 Registry Safety: PASS

**Approval:** pending
