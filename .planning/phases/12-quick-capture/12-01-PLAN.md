---
phase: 12-quick-capture
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/QuickCaptureFAB.tsx
  - src/App.tsx
  - src/pages/PublicTicketForm.tsx
autonomous: true
requirements:
  - QCAP-01
  - QCAP-02

must_haves:
  truths:
    - "User can submit a ticket by typing only a title in the FAB popover — no other fields required"
    - "After quick-create submit, user stays on current page and sees a success toast with link to new ticket"
    - "FAB button is visible on all authenticated pages (fixed bottom-right)"
    - "Logged-in user visiting /submit-ticket sees no name/email fields and sees an 'Inloggad som' badge"
    - "Logged-in user on public form submits via authenticated endpoint, not the public endpoint"
  artifacts:
    - path: "src/components/QuickCaptureFAB.tsx"
      provides: "FAB + Popover quick-create component"
      exports: ["QuickCaptureFAB"]
      min_lines: 60
    - path: "src/App.tsx"
      provides: "QuickCaptureFAB mounted inside AuthProvider"
      contains: "QuickCaptureFAB"
    - path: "src/pages/PublicTicketForm.tsx"
      provides: "Auth detection with conditional fields and dual submit path"
      contains: "useAuth"
  key_links:
    - from: "src/components/QuickCaptureFAB.tsx"
      to: "src/lib/api.ts"
      via: "api.createTicket call with minimal fields"
      pattern: "api\\.createTicket"
    - from: "src/components/QuickCaptureFAB.tsx"
      to: "src/contexts/AuthContext.tsx"
      via: "useAuth() for user.id and isAuthenticated guard"
      pattern: "useAuth"
    - from: "src/pages/PublicTicketForm.tsx"
      to: "src/lib/api.ts"
      via: "api.createTicket when logged in, api.submitPublicTicket when not"
      pattern: "api\\.createTicket"
---

<objective>
Implement the QuickCaptureFAB component for title-only ticket creation (QCAP-01) and add auth detection to the public ticket form (QCAP-02).

Purpose: Enable instant ticket creation from any page via a floating action button, and make the public form a quick-add shortcut for logged-in users.
Output: QuickCaptureFAB.tsx component, updated App.tsx with FAB mount, updated PublicTicketForm.tsx with auth-aware conditional rendering.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-quick-capture/12-CONTEXT.md
@.planning/phases/12-quick-capture/12-RESEARCH.md
@.planning/phases/12-quick-capture/12-UI-SPEC.md

@src/App.tsx
@src/pages/PublicTicketForm.tsx
@src/lib/api.ts
@src/contexts/AuthContext.tsx
@src/hooks/useTickets.ts

<interfaces>
<!-- Key types and contracts the executor needs -->

From src/lib/api.ts:
```typescript
export interface AuthUser {
  id: string;
  email: string;
  role: 'admin' | 'user';
}

async createTicket(ticket: Partial<TicketRow> & { customFields?: CustomFieldInput[]; template_id?: string | null }): Promise<TicketRow>
async submitPublicTicket(data: { name, email, title, description, ... }): Promise<...>
```

From src/contexts/AuthContext.tsx:
```typescript
interface AuthContextType {
  isAuthenticated: boolean;
  isLoading: boolean;
  user: AuthUser | null;
  signIn: (email: string, password: string) => Promise<{ error: string | null }>;
  signOut: () => Promise<void>;
}
// useAuth() returns AuthContextType
```

From src/hooks/useTickets.ts:
```typescript
export const ticketKeys = {
  all: ['tickets'] as const,
  lists: () => [...ticketKeys.all, 'list'] as const,
  // ...
};
```

Server-side constraint (server/src/routes/tickets.ts line 894):
```typescript
if (!description && (!customFields || customFields.length === 0)) {
  return res.status(400).json({ error: 'Either description or custom fields are required' });
}
```
Empty string `''` is falsy — quick-create MUST send `description: ' '` (single space) to pass server validation.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create QuickCaptureFAB component and mount in App.tsx</name>
  <files>src/components/QuickCaptureFAB.tsx, src/App.tsx</files>
  <read_first>
    - src/App.tsx (current routing and AuthProvider structure)
    - src/lib/api.ts (createTicket signature, lines 301-306)
    - src/contexts/AuthContext.tsx (useAuth hook, AuthUser type)
    - src/hooks/useTickets.ts (ticketKeys export for cache invalidation)
    - server/src/routes/tickets.ts (lines 883-930, POST /tickets validation — confirm description requirement)
    - .planning/phases/12-quick-capture/12-UI-SPEC.md (QuickCaptureFAB specs)
  </read_first>
  <action>
**Create `src/components/QuickCaptureFAB.tsx`** — a self-contained FAB + Popover component per D-01, D-02, D-03, D-04:

1. Import: `useState` from react, `useNavigate` from react-router-dom, `Plus`, `Loader2` from lucide-react, `useAuth` from `@/contexts/AuthContext`, `api` from `@/lib/api`, `ticketKeys` from `@/hooks/useTickets`, `useQueryClient` from `@tanstack/react-query`, `Button` from `@/components/ui/button`, `Input` from `@/components/ui/input`, `Popover`/`PopoverContent`/`PopoverTrigger` from `@/components/ui/popover`, `Tooltip`/`TooltipContent`/`TooltipTrigger` from `@/components/ui/tooltip`, `toast` from `sonner`.

2. Component `QuickCaptureFAB` (named export):
   - Call `useAuth()` — if `!isAuthenticated`, return `null` (self-guarding per research recommendation).
   - State: `open` (boolean, popover), `title` (string), `isSubmitting` (boolean), `titleError` (boolean).
   - `handleSubmit` async function:
     - If `!title.trim()`, set `titleError(true)` and return.
     - Set `isSubmitting(true)`.
     - Call `api.createTicket({ title: title.trim(), description: ' ', status: 'open', priority: 'medium', category_id: null, requester_id: user!.id })` — per D-03, auto-defaults are priority=medium, category=none, requester=current user. Description is a single space because server requires non-empty description when no customFields (server/src/routes/tickets.ts line 894).
     - On success: `queryClient.invalidateQueries({ queryKey: ticketKeys.lists() })`, close popover, clear title, show `toast.success('Arende skapat', { action: { label: 'Oppna', onClick: () => navigate('/tickets/${newTicket.id}') } })` — per D-04, stay on current page with toast link.
     - On error: `toast.error('Nagot gick fel. Forsok igen eller ladda om sidan.')`.
     - Finally: `setIsSubmitting(false)`.

3. Render structure (per UI-SPEC):
   - `<Popover open={open} onOpenChange={setOpen}>`
     - `<Tooltip>` wrapping `<PopoverTrigger>`:
       - `<Button size="icon" className="fixed bottom-6 right-6 z-50 h-14 w-14 rounded-full shadow-lg bg-primary text-primary-foreground hover:scale-105 hover:shadow-xl active:scale-95 transition-transform duration-200 animate-scale-in">` with `<Plus className="h-6 w-6" />`.
     - `<TooltipContent>Snabbt arende</TooltipContent>`
     - `<PopoverContent align="end" side="top" className="w-80 p-6">`
       - `<p className="text-base font-semibold mb-4">Nytt arende</p>`
       - `<Input autoFocus value={title} onChange={...} onKeyDown={submit on Enter} placeholder="Vad behover du hjalp med?" className={titleError ? 'border-destructive' : ''} />`
       - Error message: `{titleError && <p className="text-xs text-destructive mt-1">Ange en rubrik for att fortsatta</p>}`
       - `<Button className="w-full mt-4" onClick={handleSubmit} disabled={isSubmitting || !title.trim()}>` with loading state showing `Loader2` spinner + "Sparar..." or "Skicka in".

NOTE: All Swedish copy uses actual Swedish characters (a-ring, a-umlaut, o-umlaut) — the action text above uses ASCII for readability but the code MUST use proper Unicode: "Ärende skapat", "Öppna", "Något gick fel. Försök igen eller ladda om sidan.", "Snabbt ärende", "Nytt ärende", "Vad behöver du hjälp med?", "Ange en rubrik för att fortsätta", "Sparar...", "Skicka in".

**Update `src/App.tsx`** — mount QuickCaptureFAB inside AuthProvider scope:

1. Add import: `import { QuickCaptureFAB } from '@/components/QuickCaptureFAB';`
2. In the `App` component, add `<QuickCaptureFAB />` as a sibling of `<AppRoutes />` inside the `<AuthProvider>` block (line 126-128). The component self-guards via `useAuth()`, so no conditional wrapper needed.

Result:
```tsx
<AuthProvider>
  <AppRoutes />
  <QuickCaptureFAB />
</AuthProvider>
```
  </action>
  <verify>
    <automated>cd /Users/anton/Library/CloudStorage/OneDrive-Prefabmästarna/Dokument/Projekt/it-system && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - src/components/QuickCaptureFAB.tsx exists and exports `QuickCaptureFAB`
    - QuickCaptureFAB.tsx contains `useAuth()` call for self-guarding
    - QuickCaptureFAB.tsx contains `api.createTicket` call (not useTickets().addTicket)
    - QuickCaptureFAB.tsx contains `description: ' '` (single space, not empty string)
    - QuickCaptureFAB.tsx contains `priority: 'medium'` and `category_id: null` (per D-03)
    - QuickCaptureFAB.tsx contains `ticketKeys.lists()` for cache invalidation
    - QuickCaptureFAB.tsx contains `toast.success` with action containing navigate call (per D-04)
    - QuickCaptureFAB.tsx contains `fixed bottom-6 right-6 z-50` positioning
    - QuickCaptureFAB.tsx contains `autoFocus` on the Input
    - QuickCaptureFAB.tsx contains `onKeyDown` handler for Enter key submission
    - src/App.tsx contains `import { QuickCaptureFAB }`
    - src/App.tsx contains `<QuickCaptureFAB />` inside the AuthProvider block
    - TypeScript compiles without errors
  </acceptance_criteria>
  <done>QuickCaptureFAB component created and mounted. FAB appears on all authenticated pages, opens popover with title input, submits minimal ticket via api.createTicket, shows success toast with link.</done>
</task>

<task type="auto">
  <name>Task 2: Add auth detection to PublicTicketForm with conditional fields and dual submit path</name>
  <files>src/pages/PublicTicketForm.tsx</files>
  <read_first>
    - src/pages/PublicTicketForm.tsx (full file — current form structure, handleSubmit, state, render)
    - src/contexts/AuthContext.tsx (useAuth hook availability)
    - src/lib/api.ts (createTicket vs submitPublicTicket signatures)
    - .planning/phases/12-quick-capture/12-UI-SPEC.md (InloggedBadge specs, public form logged-in state)
    - .planning/phases/12-quick-capture/12-RESEARCH.md (Pattern 3, Pitfall 4, open question 2 about D-06)
  </read_first>
  <action>
**Modify `src/pages/PublicTicketForm.tsx`** to detect auth state and adapt the form per D-05 and D-06:

1. **Add imports:**
   - `import { useAuth } from '@/contexts/AuthContext';`
   - `import { User } from 'lucide-react';` (add to existing lucide import line)

2. **Add auth detection** near top of component (after existing state declarations):
   ```tsx
   const { user } = useAuth();
   const isLoggedIn = !!user;
   ```
   NOTE: `useAuth()` is safe here because `PublicTicketForm` is rendered inside `<AuthProvider>` in App.tsx (the `/submit-ticket` route is inside `<BrowserRouter><AuthProvider><AppRoutes /></AuthProvider></BrowserRouter>`).

3. **Per D-06 — when logged in, show title-only quick-create form:**
   When `isLoggedIn` is true, the public form becomes a quick-create form matching FAB behavior:
   - Hide name/email fields using `className="hidden"` on their wrapper div (keep in DOM per UI-SPEC interaction states).
   - ALSO hide description, category, priority, template, and file upload fields — D-06 says "title-only with same auto-defaults as FAB."
   - Show InloggedBadge above the title field:
     ```tsx
     {isLoggedIn && (
       <div className="inline-flex items-center gap-1.5 bg-primary/15 text-primary border border-primary/30 rounded-full px-3 py-1 text-sm">
         <User className="h-3.5 w-3.5" />
         Inloggad som {user!.email.split('@')[0]}
       </div>
     )}
     ```
   - The firstName is derived from email by splitting on `@` and taking first part (per research Pitfall 4, AuthUser has no `name` field).

4. **Modify handleSubmit** to branch on auth state:
   When `isLoggedIn`:
   - Call `api.createTicket({ title: formData.title.trim(), description: ' ', status: 'open', priority: 'medium', category_id: null, requester_id: user!.id })` — same auto-defaults as FAB per D-06.
   - Do NOT call `api.submitPublicTicket` — that endpoint requires name/email and does a contact lookup (per research anti-pattern).

   When NOT logged in:
   - Keep existing `api.submitPublicTicket(...)` call unchanged.

5. **Form validation adjustment** for logged-in path:
   - When `isLoggedIn`, only validate that `title` is non-empty. Skip name/email/description validation.
   - When not logged in, keep existing validation unchanged.

6. **Success state** remains the same (show success message) for both paths.
  </action>
  <verify>
    <automated>cd /Users/anton/Library/CloudStorage/OneDrive-Prefabmästarna/Dokument/Projekt/it-system && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - PublicTicketForm.tsx contains `import { useAuth } from '@/contexts/AuthContext'`
    - PublicTicketForm.tsx contains `const { user } = useAuth()` or equivalent destructuring
    - PublicTicketForm.tsx contains `const isLoggedIn = !!user`
    - PublicTicketForm.tsx contains `Inloggad som` text in an InloggedBadge element
    - PublicTicketForm.tsx contains `user!.email.split('@')[0]` or `user.email.split('@')[0]` for firstName derivation
    - PublicTicketForm.tsx contains `api.createTicket` call in the logged-in submit path
    - PublicTicketForm.tsx contains `requester_id: user` in the createTicket call
    - PublicTicketForm.tsx contains `description: ' '` in the logged-in submit path
    - PublicTicketForm.tsx contains `className` with `hidden` for name/email fields when logged in
    - PublicTicketForm.tsx still contains `api.submitPublicTicket` for the non-logged-in path
    - TypeScript compiles without errors
  </acceptance_criteria>
  <done>Public form detects logged-in user, hides name/email (and other fields per D-06), shows auth badge with derived firstName, and submits via authenticated endpoint with auto-defaults.</done>
</task>

</tasks>

<verification>
1. TypeScript compiles: `npx tsc --noEmit` passes with zero errors
2. Dev server starts: `npm run dev` launches without crash
3. QuickCaptureFAB renders on authenticated pages (visible in browser at bottom-right)
4. Public form at /submit-ticket shows auth badge when token exists in localStorage
</verification>

<success_criteria>
- FAB button visible on all authenticated pages (fixed bottom-right, 56px, primary blue)
- Clicking FAB opens popover with title input + submit button
- Submitting with title creates a ticket via api.createTicket with auto-defaults (priority=medium, category=null, description=' ')
- Toast appears after submit with "Ärende skapat" and "Öppna" link to new ticket
- User stays on current page after submit (no navigation)
- Public form at /submit-ticket: logged-in user sees "Inloggad som [name]" badge, no name/email fields, title-only quick-create
- Public form at /submit-ticket: non-logged-in user sees normal form unchanged
</success_criteria>

<output>
After completion, create `.planning/phases/12-quick-capture/12-01-SUMMARY.md`
</output>
