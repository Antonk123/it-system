# Environment files (innehåller hemligheter)
.env
.env.local
.env.*.local

# Databasbackuper (innehåller verklig data)
backup/

# Server runtime data (databas, uploads, backups)
server/data/

# Känsliga dokument (interna anteckningar, projektinstruktioner)
TODO.md
CLAUDE.md

# Claude Code-konfiguration (lokala/personliga inställningar ignoreras)
.claude/settings.local.json
.claude/worktrees/
.claude/agent-memory/
.claude/session-env/
.claude/sessions/
.claude/projects/

# Lokala verktygsartefakter (superpowers/GSD-plugins) — versionshanteras inte
.superpowers/
docs/superpowers/

# GSD-sketcher (throwaway UI-mockups) — versionshanteras inte
.planning/sketches/

# Dev-miljö specifika filer (hot-reload, dev-setup)
# OBS: docker-compose.dev.portainer.yml spåras numera medvetet som den
# versionsspårade källan för dev-Portainer-stacken (it-system-dev / id 40).
# Portainer-GUI:t måste spegla den filen — se docs/dev-db-isolation-runbook.md.
docker-compose.dev.yml

# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.tsbuildinfo
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
._*
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
.mcp.json
