# IT Ticket System - Konfigurationsmall
# Kopiera denna fil till .env och fyll i dina värden.
# Eller kör: bash setup.sh (genererar .env automatiskt)

# --- Portar ---
FRONTEND_PORT=8082
BACKEND_PORT=3002

# --- App-URL ---
# Används för CORS och e-postlänkar.
# Sätt till din domän eller IP + port.
CORS_ORIGIN=http://localhost:8082
APP_BASE_URL=http://localhost:8082

# --- JWT ---
# Generera med: openssl rand -base64 32
JWT_SECRET=

# --- SMTP (valfritt) ---
# Lämna tomt om e-postnotifieringar inte behövs.
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
EMAIL_FROM=
EMAIL_TO=
